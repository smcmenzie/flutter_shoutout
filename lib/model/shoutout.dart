import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';



class ShoutOut {
  String message;
  // todo:  Color messageColor;
  // Color backgroundColor;
  // DateTime dateCreated; 
  // DateTime lastUpdated;
  // bool isFavourite; // for future use
  // bool canFlash;
  // Text messageGroup; // might use in the future to group messages

  ShoutOut(this.message);

}